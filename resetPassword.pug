- var pageTitle = "Reset Password";

doctype html
html(lang="en")
  head
    include includes/meta.pug
    include includes/style.pug
    title #{pageTitle}
  body
    #emad-app
      .containers
        .left-container
          .content
            h2 Forgot Password?
            h3 Don't worry, enter your email and we will send you mail to reset it.
            a(href="signIn.html")
              button(type="submit") Back to sign in page
        .right-container
          img(class="logo", src="../img/logo2.png",alt="Logo")
          form(action="", method="post", @submit="validateForm")
            h1 Reset password
            .form-email
              input(type="text", name="resetEmail", autocomplete="off", placeholder="Type Your Email", v-model="resetEmail")
              label Your Email
            button(class="send", type="submit" :disabled="!resetEmail")
              span Reset password
            .error(v-for="error in formErrors") {{ error }}

    include includes/scripts.pug